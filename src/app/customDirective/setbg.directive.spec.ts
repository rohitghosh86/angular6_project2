import { SetbgDirective } from './setbg.directive';
import { ElementRef } from '@angular/core';

describe('SetbgDirective', () => {
  it('should create an instance', () => {
    const directive = new SetbgDirective();
    expect(directive).toBeTruthy();
  });
});
